<%- include('../_layouts/header') %>

    <%- include('../_layouts/navbar2') %>
        <div class="container">
            <%- messages('messages', locals) %>
                <% if(errors) { %>
                    <% errors.forEach(function (err) { %>
                        <div class="alert alert-danger">
                            <%= err.msg %>
                        </div>
                        <% }); %>
                            <% } %>
        </div>